---
name: michelle-cover
description: 生成高点击率的小红书 / 知识博主视频封面（YouTube Thumbnail 风格）。基于用户提供的真人照片做图生图：真人 IP 放右侧、超大标题占左侧、奶油黄重点词、白字黑描边、自然办公背景、不过度美颜。当用户要做「封面 / 缩略图 / thumbnail / 小红书知识博主封面 / AI 博主封面」时使用。
---

# AI 知识博主封面 Skill

一套把「真人照片 → 高点击率封面」标准化的公式。核心不是设计感，是**点击率**：在小红书两列信息流的缩略图尺寸下，用户 1 秒内要能看清标题、认出人物、判断主题。

## 什么时候用

用户想为知识类账号（尤其 AI / 转行 / 效率赛道）做视频封面、缩略图、thumbnail 时。

## 需要用户提供

1. **人像照片**（必需）——真人 IP，用于做图生图，不改变人物身份。
2. **标题文字**（必需）——一句话即可，含 1 个重点词和 1 个数字，skill 会自动拆成 4–5 行短句排版。
3. **参考封面图**（可选）——已有的同风格成品，用于风格锚定。

缺人像照片时先向用户索要，不要用纯 AI 生成的假脸替代。

## 配合的图生图工具

这个 skill 负责**出提示词 + 校验**，本身不生图。真正的生成要交给一个支持「参考图 / 图生图（image-to-image）」的图像模型——把你的人像照片作为参考图传进去，模型才能在保持人物身份的前提下重绘成封面。

用法：上传人像照片（可再加 1 张同风格参考封面）→ 粘贴下方主提示词 → 生成 → 按自检清单校验。

## 生成流程

1. 读入用户的人像照片 + 标题文字。
2. 把标题拆成 **4–5 个短句/词组**，规划每句的字号大小（数字/收尾重点句最大，普通连接词最小），并决定哪几句要加**色块高亮**（奶油黄色块或白色圆角色块压在文字下面）。
3. 套用下方「主提示词」，把占位符替换成用户的实际内容。
4. 调用图生图模型（image-to-image），输入 = **用户人像照片 + `assets/` 目录下 1–2 张示例封面图（如 cover-01.jpg、cover-03.jpg）+ 主提示词**。把示例封面图一起作为参考图传入，能让模型直接照抄版式/字号节奏/色块高亮方式，比纯文字描述更准。用户自己提供参考封面图时，优先用用户的那张替换/补充默认示例图。
5. 出图后，逐条跑「出图自检清单」，不达标就调提示词重生成。

## 主提示词（把 {} 替换后使用）

> 设计一张小红书知识博主封面，风格必须严格参照参考图（assets 示例封面 / 用户提供的参考图）的版式：文字字号节奏、色块高亮方式、人物与文字的左右占比，都以参考图为准，不要自由发挥。使用我提供的人像照片，不改变人物身份，不要过度美颜，仅轻微优化皮肤（去除细纹、痘印、黑眼圈），脸型略瘦、下颌线更清晰，但保持真实自然的人物比例。
>
> **左右各占画面宽度的一半**：左侧文字区域宽度约为画面的 50%，不能超过画面竖直中线；右侧人物区域占剩下的 50%。人物是**近景大特写**，头部到身体占满画面上下大部分高度（头顶留一点点自然呼吸空间即可，不用死贴边），不要留大片背景空白，不要缩小成「站在画面中间、四周空荡荡」的全身小尺寸人像。人物完整露出，不遮挡标题。背景保留真实办公、咖啡馆或自然光环境，且要与人物自然融合过渡——光影方向、色温、虚化程度和人物本身保持一致，不能有抠图边缘感或拼贴感，不使用科技感背景。
>
> 左侧标题**拆成 4–5 行短句**，从画面顶部一路铺到接近底部，占满整个左侧竖向空间，不能只挤在左上角、下方留白。**每一行的字号要有明显的大中小差异**，形成强烈层次节奏（禁止全篇同一字号）。**重点短句要用「色块高亮」强调，不是只改文字颜色**：给关键词/关键短句加一块奶油黄色块（#F6D66B）或白色圆角色块压在文字底下，文字叠在色块上方，参考图里「转行AI岗位」「有壁垒」「天花板」这种色块压字的效果。普通连接词就用白字配粗黑描边、不加色块、字号偏小。数字「{数字}」用橙色文字、不加色块，字号是全图最大。重点词「{重点词}」用奶油黄色块高亮 + 黑描边文字。标题内容为：{标题文字}。所有标题必须完整显示，不能被人物遮挡，尤其 'AI' 必须完整露出。
>
> 整体配色干净、高级、暖色调，少量黄色笔刷、箭头、星星等漫画元素点缀（5 个以内）。画质像 iPhone 自然拍摄，避免 AI 感、HDR 感和夸张磨皮。
>
> **优先级约束**：优先保证点击率而非设计感——标题可读性第一，人物辨识度第二，装饰元素第三。整张封面在小红书两列流缩略图尺寸下，用户 1 秒内要能看清标题、认出人物与主题。左右严格各占一半，画面不能有大片「什么都没有」的空白背景。

完整分模块规范（人物 / 标题 / 排版 / 背景 / 颜色 / 画质 / 反面清单）见 [references/style-guide.md](references/style-guide.md)。

## 排版骨架（左文字 50% / 右人物 50%）

以标题「24小时内，找到真正适合你的转行AI岗位」为例：

```
——————————————————————————————————
【超大橙|无色块】24 【中黑|无色块】小时内，   |
【中黑|无色块】找到真正                        |   [ 人物特写
【中黑|无色块】适合你的                        |     占右侧50%宽度
【大黄色块】转行                               |     上下几乎占满画面
【超大黄色块】AI 岗位                          |    （头顶留一点呼吸空间）]
——————————————————————————————————
        左侧 50% 宽度                          右侧 50% 宽度
```

拆句规则：标题拆成 **4–5 行**，每行是一个独立短句/词组；字号按「数字/收尾重点句最大 → 中间短句中等 → 连接词最小」排布，**禁止全篇同一字号**。最后 1–2 句（通常含重点词）用**色块高亮**（奶油黄色块或白色圆角色块压字），前面的句子用纯文字（白字黑描边）即可，不必每句都加色块。

铁律：
- 文字不压人物、文字不盖脸、人物不挡标题
- **左右严格各占 50%**：文字区域不超过画面竖直中线，人物区域占剩下的 50%
- 左侧文字铺满（顶到底），右侧人物铺满（占满上下大部分高度，头顶留少量自然呼吸空间即可）
- 重点短句用色块高亮压字，不是只改文字颜色
- 反面例子（禁止）：文字区域超过画面一半、挤压人物空间；人物是缩小的全身照、四周留白很多；标题只有 2-3 行且字号一样、堆在左上角；重点词只变色不加色块

## 出图自检清单（不达标就重生成）

- [ ] 缩略图尺寸下，1 秒内能看清标题
- [ ] 'AI' 和数字完整露出，没被人物 / 身体挡住
- [ ] 左右严格各占 50%：文字没有越过画面竖直中线，人物没有被挤到很窄
- [ ] 人物是近景大特写，占满上下大部分高度，没有大片背景空白（不是缩小的全身照）
- [ ] 标题拆成 4–5 行，字号有明显大中小层次，不是全篇一个字号
- [ ] 至少 1–2 句重点短句用了色块高亮压字（不是只改文字颜色）
- [ ] 标题从顶部铺到接近底部，占满左侧竖向空间（不是只堆在左上角、下方空着）
- [ ] 皮肤是真人质感，不是塑料磨皮 / 大眼娃娃脸
- [ ] 配色暖色 / 奶油色，没有蓝紫科技感背景
- [ ] 背景与人物光影、色温自然融合，没有抠图感 / 拼贴感
- [ ] 重点词奶油黄、数字橙色，白字有厚黑描边
- [ ] 装饰元素 ≤ 5 个
- [ ] 整体像 iPhone 拍的，无 HDR / 过曝 / AI 渲染感

## 核心公式（一句话）

左右各占 50% ＋ 真人 IP 大特写铺满右侧 ＋ 4-5 行大中小字号层次的标题铺满左侧 ＋ 重点短句色块高亮压字 ＋ 白字黑描边 ＋ 少量漫画元素 ＋ 自然办公背景 ＋ 不过度美颜 ＋ YouTube Thumbnail 排版 ＋ 小红书知识博主风格。**左右都不留大片空白，画面被人物和文字撑满。**
